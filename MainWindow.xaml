<Window x:Class="BmwCarDataClient.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.modernwpf.com/2019"
        xmlns:loc="clr-namespace:BmwCarDataClient.Localization"
        ui:WindowHelper.UseModernWindowStyle="True"
        Title="BMW Telemetry Client" Height="1050" Width="1000"
        WindowStartupLocation="CenterScreen">

    <Grid>
        <ui:NavigationView x:Name="MainNavigation" 
                           PaneDisplayMode="Auto"
                           IsBackButtonVisible="Collapsed"
                           ItemInvoked="MainNavigation_ItemInvoked"
                           Loaded="MainNavigation_Loaded">
            
            <ui:NavigationView.MenuItems>
                <ui:NavigationViewItem Content="{Binding Source={x:Static loc:TranslationSource.Instance}, Path=[Nav_Account]}" Tag="Account" Icon="Contact" />
                <ui:NavigationViewItem Content="{Binding Source={x:Static loc:TranslationSource.Instance}, Path=[Nav_Dashboard]}" Tag="Dashboard" Icon="Play" />
                <ui:NavigationViewItem Content="{Binding Source={x:Static loc:TranslationSource.Instance}, Path=[Nav_VehicleInfo]}" Tag="VehicleInfo" Icon="Document" />
            </ui:NavigationView.MenuItems>

            <ui:NavigationView.FooterMenuItems>
                <!-- Moved Containers to Footer as an Admin/Setup task -->
                <ui:NavigationViewItem Content="{Binding Source={x:Static loc:TranslationSource.Instance}, Path=[Nav_Containers]}" Tag="Containers" Icon="Library" />
            </ui:NavigationView.FooterMenuItems>

            <Frame x:Name="ContentFrame" NavigationUIVisibility="Hidden" />
            
        </ui:NavigationView>
    </Grid>
</Window>
